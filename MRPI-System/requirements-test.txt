# Dépendances nécessaires pour exécuter la suite de tests (en plus des
# dépendances runtime déjà requises par le projet : fastapi, pandas,
# numpy, scikit-learn, openai, python-dotenv, uvicorn).
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # requis par fastapi.testclient.TestClient
